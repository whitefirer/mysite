# Project Rules

- NEVER `git push` without explicit user approval. Ask first, wait for confirmation.
- Always present diffs for review before committing.
- For theme changes, commit in the theme submodule first, then update the parent submodule reference.
